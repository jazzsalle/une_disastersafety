# Pipeline dependencies (Python 3.11)
# NOTE: comments are ASCII-only so pip works on cp949 consoles (Windows).
PyMuPDF>=1.24          # PDF text extraction (poppler-based tools drop Korean glyphs - do not use)
pdfplumber>=0.11       # PDF table extraction
openpyxl>=3.1          # Read Excel files (disaster meta definition sheets)
requests>=2.32         # HTTP calls (VWorld geocoder etc.)
