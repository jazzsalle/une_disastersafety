# 원시데이터 (대용량 보고서·원본 자료 — 저장소에 올리지 않음)
자연재해저감종합계획 및 하천기본계획/
재난 메타 데이터 정의서_재난판단기준/

# 사업 내부 문서 (참여인력 실명 등 포함 — 원본은 저장소에 올리지 않음, 분석 결과는 docs/00 참조)
*사업수행계획서*.pdf

# 글꼴 바이너리 (POC 앱에 필요한 사본은 app 쪽 public/fonts에 별도 배치)
SpoqaHanSansNeo-Regular/

# 빌드·의존성
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# 환경·비밀
.env
.env.*
!.env.example

# 데이터 파이프라인 산출물(대용량) — 단 런타임 필수 5종은 예외(Vercel 배포 번들용)
data/processed/*
!data/processed/chunks.jsonl
!data/processed/criteria.json
!data/processed/districts.json
!data/processed/rivers.json
!data/processed/geo.json
data/index/

# OS/편집기
.DS_Store
Thumbs.db
.vercel

# Claude Code 로컬 아티팩트(런타임 락파일만 제외 — agents·skills 구성은 커밋 대상)
.claude/scheduled_tasks.lock
