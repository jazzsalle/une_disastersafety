Tab item — selected, size lg|md|sm, optional leftIcon/badge. TabLine/TabFill are the line- and fill-style tab bars (prop=label, prop2=mode, prop3=size, prop4=state).
```jsx
<Tab label="개요" selected size="md" />
```
