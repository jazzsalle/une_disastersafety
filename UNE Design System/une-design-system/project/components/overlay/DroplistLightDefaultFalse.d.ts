import * as React from 'react';
export interface DroplistLightDefaultFalseProps {
  className?: string;
  style?: React.CSSProperties;
  prop?: boolean;
  prop2?: string;
  /** Swappable nested instance; defaults to the design's. */
  icon1?: React.ReactNode;
}
export declare const DroplistLightDefaultFalse: React.FC<DroplistLightDefaultFalseProps>;
export default DroplistLightDefaultFalse;
