Segmented-control segment — intent none|primary, selected, size lg(40)|md(36)|sm(32). Compose several in a row.
```jsx
<Segment intent="primary" selected lablel="목록" size="md" />
```
Note: label prop is spelled `lablel` (source layer name).
