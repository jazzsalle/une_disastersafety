Primary action button — fill/outline/ghost × primary/grayscale, 10 sizes (3xl 56px → 4xs 20px).
```jsx
<Button variant="fill" color="primary" size="md" label="확인" />
```
States: default/hover/active/focus-visible/disabled via `state`. `leftIcon`/`rightIcon` toggle icon slots.
