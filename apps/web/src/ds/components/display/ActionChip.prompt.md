Chips: ActionChip (tappable action), ChoiceChip (single choice, selected), FilterChip (leftIcon + selected), InputChip (removable). All: variant fill|outline|ghost, size lg|md|sm.
```jsx
<FilterChip variant="outline" size="md" label="필터" leftIcon />
```
