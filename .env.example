# 환경변수 템플릿 — 실제 값은 이 파일을 .env로 복사해 기입 (.env는 gitignore 대상)

# VWorld 인증키 (www.vworld.kr 발급 — 하나의 키를 두 변수에 공용 기입)
# 프론트엔드(Vite) 지도 타일용
VITE_VWORLD_API_KEY=
# 파이프라인(지오코더·2D데이터 API)용
VWORLD_API_KEY=

# Claude API 키 (선택 — 미설정 시 mock 응답 모드로 동작)
ANTHROPIC_API_KEY=
